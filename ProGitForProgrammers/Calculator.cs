using System;
namespace ProGitForProgrammers
{
    class Calculator 
    {
        //added the add method"
        public int Add(int left, it right) 
        {
            return left + right;
        }
        public int Subtract(int left, it right) 
        {
            return left - right;
        }
    }
}